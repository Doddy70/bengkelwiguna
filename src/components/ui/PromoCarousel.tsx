"use client";

/**
 * PromoCarousel Wrapper
 * Re-export the new PromoCarousel component from apple-cards-carousel.tsx
 * For backward compatibility
 */

export { default } from './apple-cards-carousel';
